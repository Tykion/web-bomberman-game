{
  "dependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "bomb-me": "concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "start:frontend": "cd client && npm install && npm run dev",
    "start:backend": "cd server && npm install && node app.js"
  }
}
